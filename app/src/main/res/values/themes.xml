<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AOMA" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor" tools:targetApi="1">@color/ic_launcher_background</item>
        <item name="android:navigationBarColor" tools:targetApi="1">@color/ic_launcher_background</item>
    </style>

    <style name="Theme.AOMA" parent="Base.Theme.AOMA" />
</resources>